language: python
python:
  - "3.6.1"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  #- python setup.py test
  - py.test Particle_Simulation/tests/ --cov=codecov
after_success:
  - codecov
